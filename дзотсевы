#include <iostream>
#include <thread>
using namespace std;
    void f(){
        thread::id t= this_thread::get_id();
    for(int i=1; i<10000;i++){
        cout <<i << " " << t <<"\n";
    }
    }
int main(){

thread x(f),y(f),z(f);
        x.join();
         z.join();
     y.join();
}
